<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <!-- Link to Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  </head>
  <body>
    <!-- Header section containing navigation bar -->
    <header>
    <!--- Navigation-->
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand fw-bold text-primary-emphasis fs-3" href="/">Tech Blog</a>
        <!-- Button for toggling the navigation menu on smaller screens -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
          <!-- Navigation links -->
          <ul class="navbar-nav">
            <!-- Home link, active if on the home page -->
            <li class="nav-item {{#if isHome}}active{{/if}}">
              <a class="nav-link text-primary" href="/">Home
                <span class="sr-only"></span></a>
            </li>
            <!-- Dashboard link, conditional based on user login status -->
            <li class="nav-item {{#if isDash}}active{{/if}}">
              <a class="nav-link text-primary" {{#if logged_in}}href="/dashboard"{{/if}}{{#unless logged_in}}href="/login"{{/unless}}>Dashboard</a>
            </li>
            <!-- Login link, only shown if user is not logged in -->
            {{#unless logged_in}}
              <li class="nav-item {{#if isLogin}}active{{/if}}">
                <a class="nav-link" href="/login">Login</a>
              </li>
            {{/unless}}
          </ul>
          <!-- Logout button, only shown if user is logged in -->
          <ul class="navbar-nav">
            {{#if logged_in}}
              <li class="nav-item">
                <button class="nav-link logout btn bg-secondary text-light" id="logout">Logout</button>
              </li>
            {{/if}}
          </ul>
        </div>
      </nav>
    </header>

    {{{body}}}

    <!-- Include jQuery library (required for Bootstrap’s JavaScript plugins) -->
    <script
      src="https://code.jquery.com/jquery-3.2.1.slim.min.js"
      integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN"
      crossorigin="anonymous"
    ></script>
    <!-- Include Popper.js library (required for Bootstrap’s tooltips and popovers) -->
    <script
      src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"
      integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q"
      crossorigin="anonymous"
    ></script>
    <!-- Include Bootstrap JavaScript library for interactive components -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"
      integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl"
      crossorigin="anonymous"
    ></script>
    
    <!-- Include logout script, only if the user is logged in -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
  </body>
  
</html>
